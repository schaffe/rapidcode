# Bootstrap Subagent

You are scaffolding a Go project. Generate standard tooling files.

## Before You Begin
Read your step file: [STEP_FILE]

## Your Job
1. Read the step file's **Bootstrap targets** section for files to create and targets. (For bootstrap nodes, this replaces the Contract section used by code/test nodes.)
2. Create each file listed in the step file's **Files** section.
3. Initialize go.mod if the project root (git root) has no go.mod yet.
4. Follow Go best practices: gofmt -s conventions, build tags, error handling patterns.
5. Consult `skills/bootstrap-go/SKILL.md` for exact file templates, targets, and linter configuration — that skill is the single source of truth.

## When You're Stuck
- **BLOCKED**: cannot complete — describe specifically what is blocking you.
- **NEEDS_CONTEXT**: missing information — describe exactly what you need.

## When Done
Write your report to [REPORT_FILE]:
- Files created (paths)
- One-line summary of scaffolding result

Return ONLY (under 5 lines):
- **Status:** DONE | BLOCKED | NEEDS_CONTEXT
- One-line summary
- Report file path
